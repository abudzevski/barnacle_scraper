class UpdateListings:
    def __init__(self, meta_repo, items_repo,network):
        self.meta_repo = meta_repo
        self.items_repo = items_repo
        self.network = network

    def __call__(self):
        #do stuff here.
        None